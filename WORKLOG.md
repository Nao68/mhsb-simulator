# MHSB 作業メモ

## このフォルダの概要

このプロジェクトは、Monster Hunter Rise: Sunbreak 向けの装備・ダメージシミュレーターです。

- `index.html`: 本体シミュレーター画面
- `app.js`: 本体ロジック
- `styles.css`: 本体画面のスタイル
- `missing-data.html`: 未設定データ入力ツール
- `missing-data.js`: 未設定データ入力ツールのロジック
- `missing-data.css`: 未設定データ入力ツールのスタイル
- `build_data.py`: `mhrice.json` などから `data.js` を生成するスクリプト
- `data.js`: 本体が使う生成済みデータ
- `mhrice.json`: 元データ
- `mhrice.full.backup.json`: 大きいバックアップ元データ

## 現在入っている主な機能

- 武器、防具、護石の選択
- 装飾品、百竜装飾品の選択
- 合計スキルの表示
- ダメージ概算
- 武器傀異錬成の反映
- 防具/護石向けの追加スキル・スロット編集
- 護石の保存、更新、削除
- 装備プリセットの保存、更新、適用、削除
- 未設定データ入力ツールでの手動補完
  - 武器傀異錬成
  - ボウガン弾設定
  - JSON エクスポート/インポート
  - 本体シミュレーターへの反映

## localStorage の保存キー

- `mhsb_presets_v1`: 装備プリセット
- `mhsb_last_state_v1`: 最後の本体画面状態
- `mhsb_charms_v1`: 保存した護石
- `mhsb_missing_data_draft_v1`: 未設定データ入力ツールの下書き
- `mhsb_missing_data_applied_v1`: 本体へ反映済みの未設定データ

## 確認済み

- Node.js はインストール済み。
  - ユーザー環境では `node -v` が `v24.16.0`
  - `npm -v` が `11.13.0`
- Codex 側の PATH にはまだ Node.js が反映されていない場合がある。
  - その場合は `C:\Program Files\nodejs\node.exe` を直接使う。
- 次の構文チェックは通過済み。
  - `app.js`
  - `missing-data.js`
  - `data.js`
- `build_data.py` は `python -m py_compile build_data.py` を通過済み。
- PowerShell で文字化けして見える場合があるが、UTF-8 指定で読むと日本語は正常。

## 2026-05-29 追加作業

- `missing-data.html` の保存方式説明に、本体側の反映先を追記。
- `missing-data.js` の未設定データ入力ツールに、人間が扱いやすい注釈を追加。
  - 武器傀異錬成の保存フロー説明
  - 武器傀異錬成テーブルの各欄が本体のどこへ反映されるかの説明
  - ボウガン弾設定の保存フロー説明
  - 反動、装填速度、ブレ、特殊弾、速射の本体反映先説明
  - 弾リストの各欄が本体の「ボウガン詳細」にどう表示されるかの説明
- `missing-data.css` に注釈表示用のスタイルを追加。
- `missing-data.js` と `app.js` は Node.js の構文チェックを通過済み。

## 2026-05-30 追加作業

- 未設定データ入力ツールの武器傀異錬成テーブルを整理。
- `slotBonus.*` は本体ロジック上「傀異スロット拡張」を選んだ時だけ使われるため、傀異スロット拡張カードにだけ表示するように変更。
- 攻撃力強化、会心率強化、切れ味強化、属性/状態異常系、百竜スロット拡張は、`Lv` 表示 + `Cost` + `値` のシンプルな表に変更。
- `Lv` は基本固定扱いとして入力欄ではなく表示チップに変更。
- 行追加時は既存の最大 Lv の次の Lv を自動で追加。
- シンプル表と傀異スロット拡張表の横幅を分けて、通常カテゴリが横に広がりすぎないように調整。
- `missing-data.js` と `app.js` は Node.js の構文チェックを通過済み。

## 2026-07-01 追加作業

- Claude と共同開発する可能性に備えて `CLAUDE_HANDOFF.md` を追加。
- Claude 向けに、最初に読むファイル、共同開発ルール、Git 運用、注意ファイル、確認コマンド、確認が必要な場面を整理。

## 2026-07-01 追加作業（Claude提案を反映）

- `claude/missing-data-ui` ブランチで、Claude から受け取った `missing-data` UI 改善案を反映。
- `missing-data.js` の武器傀異錬成カード表示を整理。
  - この武器で基準データがあるカテゴリと、ないカテゴリを分離。
  - 基準データがないカテゴリは `<details>` でまとめて折りたたみ表示に変更。
  - カード見出しの「基準カテゴリID」「内部キー」表示を `<details class="augment-tech-info">` に格納。
  - 武器情報カードに、この武器で実際に使うカテゴリ名をチップ表示する `getUsedAugmentLabels` を追加。
- `missing-data.css` に `.augment-unused-block` と `.augment-tech-info` のスタイルを追加。
- `missing-data.html` の保存方式説明に、未使用項目の折りたたみ表示についての一文を追加。
- `data.js` / `build_data.py` / localStorage キーは変更なし。

## 次にやるとよさそうなこと

1. ブラウザで `index.html` を開いて本体画面を動作確認する。
2. 装備選択、装飾品、護石保存、プリセット保存が期待通り動くか確認する。
3. `missing-data.html` を開いて、未設定データの保存と本体反映を確認する。
4. ボウガン弾設定の反映結果が本体側の表示に出るか確認する。
5. ダメージ計算が装備・スキル・錬成値を反映しているか確認する。
6. 不具合が出たら、コンソールエラーと操作手順をもとに修正する。

## 注意

このフォルダは今のところ git リポジトリではありません。変更履歴が必要なら、作業前に git 初期化を検討してください。
